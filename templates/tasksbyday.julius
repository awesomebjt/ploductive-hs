function goToSelectedDay(chosenDate) {
    if ((new Date(chosenDate) !== "Invalid Date") && !isNaN(new Date(chosenDate)))
    {
        location.href = "/tasks/day/"+chosenDate;
    }
    else
    {
        alert("Invalid date!");
    }
};

function toggleDone(taskId) {
    alert(taskId);
}